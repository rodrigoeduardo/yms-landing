# Data Model

<!-- Entities, fields, relationships, invariants. Reviewers verify migrations and
     queries against the invariants listed here. -->

## Entities

<!-- One section per entity: fields (name, type, constraints), relationships. -->

## Invariants

<!-- Cross-entity truths that must always hold, e.g. "an order always references an
     existing customer", "quantity is never negative". -->
